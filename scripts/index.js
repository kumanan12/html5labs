import { add,sub,divide } from "./my-math.js";

console.log(add(2,3));
console.log(sub(3,2));
console.log(divide(12/3));
for (var pair of data1.entries()) {
    console.log(pair[0] + ": " + pair[1]);
  }

  for (var pair of data1.entries()) { console.log(pair[0] + ": " + pair[1]);  }